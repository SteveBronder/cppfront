#include "deep_c.h2"

// Middle level - includes deep_c.h2
deep_type_b: type = {
    c: deep_type_c = ();
    operator=: (out this) = { }
    get_value: (this) -> int = 200;
    get_c_value: (this) -> int = c.get_value();
}
